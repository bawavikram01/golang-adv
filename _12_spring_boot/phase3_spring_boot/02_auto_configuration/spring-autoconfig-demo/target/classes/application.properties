server.port=8081
spring.application.name=autoconfig-demo

# Expose conditions endpoint to inspect auto-configuration decisions
management.endpoints.web.exposure.include=conditions,beans,env,info,health
management.endpoint.health.show-details=always
