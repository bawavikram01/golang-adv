# Application name
spring.application.name=spring-demo

# Disable the web server (we're just learning IoC, no REST yet)
spring.main.web-application-type=none

# Disable the Spring banner for cleaner output
spring.main.banner-mode=off
