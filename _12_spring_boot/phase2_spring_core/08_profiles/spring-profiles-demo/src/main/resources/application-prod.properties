# ─── PROD Profile ─────────────────────────────────────────────
# Overrides base application.properties when profile=prod

app.database.type=PostgreSQL
app.database.url=jdbc:postgresql://prod-server:5432/myapp

app.cache.enabled=true
app.log-level=WARN
app.mock-external-services=false
app.dev-tools=false
