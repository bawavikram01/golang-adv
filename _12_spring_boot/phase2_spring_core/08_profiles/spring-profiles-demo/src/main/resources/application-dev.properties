# ─── DEV Profile ──────────────────────────────────────────────
# Overrides base application.properties when profile=dev

app.database.type=H2 (In-Memory)
app.database.url=jdbc:h2:mem:devdb;DB_CLOSE_DELAY=-1

app.cache.enabled=false
app.log-level=DEBUG
app.mock-external-services=true
app.dev-tools=true
