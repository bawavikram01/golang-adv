// math.h — HEADER FILE (declarations only)
// This tells other files WHAT functions exist, not HOW they work.
// Think of it as a menu — it lists what's available.

#pragma once  // Prevent double-inclusion

// These are DECLARATIONS (also called "prototypes")
// They say: "these functions exist somewhere, here are their signatures"
int add(int a, int b);
int subtract(int a, int b);
int multiply(int a, int b);
double divide(int a, int b);
